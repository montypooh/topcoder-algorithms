#include <string>
#include <vector>
#include <utility> // pair
#include <algorithm> // sort, find, min, max
#include <cmath> // abs
#include <cassert>
#include <iostream>
#include <sstream>

using namespace std;

class RoboCourier {
  public:
    int timeToDeliver(vector <string> path) {
		
    }
};

int main() {
    RoboCourier cn;
    vector <string> path;
    int rc = cn.timeToDeliver(vector <string> path);
    cout<<rc<<endl;
}
