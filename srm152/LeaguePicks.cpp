#include <string>
#include <vector>
#include <utility> // pair
#include <algorithm> // sort, find, min, max
#include <cmath> // abs
#include <cassert>
#include <iostream>
#include <sstream>

using namespace std;

class LeaguePicks {
  public:
    vector <int> returnPicks(int position, int friends, int picks) {
		
    }
};

int main() {
    LeaguePicks cn;
    int position, int friends, int picks;
    vector <int> rc = cn.returnPicks(int position, int friends, int picks);
    cout<<rc<<endl;
    return 0;
}
