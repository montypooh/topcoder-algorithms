#include <string>
#include <vector>
#include <utility> // pair
#include <algorithm> // sort, find, min, max
#include <cmath> // abs
#include <cassert>
#include <iostream>

using namespace std;

class FoxConnection4 {
  public:
    int howManyWays(vector <string> board, int k) {
        
    }
};

int main() {
    FoxConnection4 cn;
    vector <string> board, int k;
    int rc = cn.howManyWays(vector <string> board, int k);
    cout<<rc<<endl;
    return 0;
}
